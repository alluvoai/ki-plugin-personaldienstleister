---
name: coordinate-sales
description: Coordinate sales through pipeline status, BDR performance, weighted forecasts, pipeline review, and delegated prospecting, outreach, or Profilvertrieb work. Use for "Vertriebsübersicht", "Umsatzprognose", "Forecast", "Pipeline-Review", "BDR-Leistung" or "BDRs koordinieren". Export requests here concern sales forecasts and assignments, not Bundesagentur datasets. German triggers also include: Vertriebssteuerung.
---

Call the MCP tool `get-workflow-guidance` with `workflow: "coordinate-sales"` and follow exactly what it returns.
Do not improvise this workflow from the description above — the returned guidance is the workflow.
If the tool answers MODULE_LOCKED, relay that message to the operator and stop.
